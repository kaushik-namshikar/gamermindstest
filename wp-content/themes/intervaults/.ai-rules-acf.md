You are generating production-ready WordPress ACF blocks using modern block.json registration.

GENERAL ARCHITECTURE RULES:
You must write output files to disk using filesystem actions (create directories + files) using the exact structure in Rule 36 and Rule 37. Do not only print code—create/update the files.

The block slug must be automatically generated based on the analyzed design/content.
It must be kebab-case, descriptive, and never literally "block-slug".
Example: hero-video-banner, testimonial-slider, feature-grid, etc.

1. Use block.json for block registration (NOT acf_register_block_type()).
2. Block must be registered using register_block_type(**DIR** . '/blocks/block-slug') in functions.php.
3. block.json must include:
   - apiVersion: 2
   - supports configuration
   - acf.renderTemplate
4. The block must fully support Gutenberg wrapper attributes via get_block_wrapper_attributes().
5. The block must behave like a native Gutenberg block.
6. All spacing, color, and alignment supports must work via block.json.
7. JS and video functionality must work in:
   - Frontend
   - Gutenberg editor (enqueue correctly)
8. Use bootstrap wherever necessary.

ACF FIELD RULES:

8. Provide ACF field group JSON (local JSON export format).
9. Field keys must be unique (field_XXXXXXXX format).
10. Field names must be prefixed with block slug.
11. Group fields logically.
12. Use correct field types (text, textarea, image, repeater, link, select, etc.).
13. Image return format must be array.
14. Link return format must be array.
15. Repeater fields must use have_rows().
16. Include proper block location rule.
17. JSON must be directly importable into ACF.

PHP RULES:

18. Use semantic markup.
19. Use get_block_wrapper_attributes() on outermost element.
20. Use esc_html(), esc_url(), esc_attr().
21. Do not hardcode content.
22. Follow WordPress coding standards.
23. Avoid unnecessary wrapper divs.
24. Ensure accessibility (alt attributes, aria where needed).

SCSS RULES:

25. Use BEM naming convention.
26. SCCS must be responsive across all screen sizes.
27. Use clamp() where appropriate.
28. Production-ready SCSS only.

JS RULES:

31. Only include JS if required.
32. JS must work in frontend and editor.
33. Enqueue via block.json (viewScript/editorScript) OR wp_enqueue_script conditionally.
34. Avoid global scope pollution.
35. Ensure videos autoplay/muted properly and reinitialize in editor if needed.

STRUCTURE RULES:

36. Block must be structured as:

blocks/{generated-block-slug}/
block.json
render.php
style.scss
script.js (if needed)

37. ACF JSON must be placed in ACF local JSON folder.
38. Code must be production-ready.
39. No deprecated methods.
40. Follow modern WordPress architecture.
