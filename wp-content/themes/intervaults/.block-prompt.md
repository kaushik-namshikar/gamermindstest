I am attaching a screenshot of a Figma section.

Follow rules defined in .ai-rules-acf.md.

Your task:

1. Analyze the screenshot structure.
2. Generate a production-ready modern ACF block using block.json registration.

Provide the following:

- block.json
- render.php (block template)
- style.scss (responsive)
- script.js (only if required)
- Example register_block_type() snippet
- ACF field group JSON (local JSON format, import-ready)

Requirements:

BLOCK REGISTRATION:

- Use block.json (apiVersion 2).
- Include supports (spacing, color, align if applicable).
- Include acf.renderTemplate.
- JS must work in frontend and editor.
- Videos must autoplay/muted correctly and function inside editor.
- Ensure wrapper attributes via get_block_wrapper_attributes().

ACF FIELD GROUP:

- Unique field keys (field_XXXXXXXX format).
- Field names prefixed with block slug.
- Proper return formats (image = array, link = array).
- Repeater if multiple items exist.
- Proper field grouping.
- Block location rule.
- JSON directly importable.

PHP:

- Use get_field(), have_rows().
- Escape all outputs.
- Use semantic markup.
- No hardcoded content.
- Use proper alt attributes.

SCSS:

- BEM naming.
- Responsive across all screen sizes.
- Use clamp() where appropriate.
- Clean production-ready structure.

JS (if required):

- Must initialize both in frontend and editor.
- Use DOMContentLoaded and wp.domReady if needed.
- Reinitialize after block render in editor (important for sliders/videos).
- Avoid global scope leakage.

OUTPUT FORMAT:

IMPORTANT: Do NOT just generate code in chat. You must create the required folders/files in the repository filesystem.

First determine an appropriate block slug based on the screenshot structure.
Use that slug consistently in:

- Folder name
- Field name prefixes
- JSON filename
- block.json name
- CSS class naming
  Do not use "block-slug" literally.

Create/update these paths exactly:

- blocks/{generated-block-slug}/block.json
- blocks/{generated-block-slug}/render.php
- blocks/{generated-block-slug}/style.scss
- blocks/{generated-block-slug}/script.js (if required)
- acf-json/{generated-block-slug}-field-group.json

After writing files, output a short manifest listing each created file path.
